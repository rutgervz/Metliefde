import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Server Components zijn standaard in App Router; geen extra config nodig in fase 1.
  experimental: {
    // Gereserveerd voor latere fase (bv. PPR).
  },
};

export default nextConfig;
